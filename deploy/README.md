# Deployment Assets

This directory contains deployment drafts for the final Tencent Cloud environment. These files are examples and checklists; they do not affect local feature development unless a developer chooses to use them.

Current assets:

- `nginx/xm4eicsa.conf.example`: Nginx virtual host and reverse proxy draft.

Keep real server passwords and private environment files outside the repository.