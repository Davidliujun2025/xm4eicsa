# token-quota-management

User story directory for token quota management.

Suggested layout:

- `ui/`: quota overview, adjustment, and alert pages.
- `server/`: quota allocation, records, and alert rules.
- `docs/`: reporting dimensions and operation rules.