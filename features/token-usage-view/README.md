# token-usage-view

User story directory for personal token usage viewing.

Suggested layout:

- `ui/`: usage dashboard and detail list.
- `server/`: usage query and aggregation logic.
- `docs/`: metrics and filter definitions.