# forgot-password

User story directory for password recovery.

Suggested layout:

- `ui/`: account verification and reset screens.
- `server/`: verification, reset token, and password update logic.
- `docs/`: flow notes and validation rules.