# create-agent-account

User story directory for creating customer-service agent accounts.

Suggested layout:

- `ui/`: account creation form and status management screens.
- `server/`: account creation, role assignment, and status updates.
- `docs/`: field definitions and permission rules.