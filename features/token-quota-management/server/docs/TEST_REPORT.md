# 假数据验证报告

验证日期：2026-07-22

执行命令：

```powershell
./scripts/test.ps1
```

结果：

```text
Build passed.
PASS: 70 assertions
All tests passed.
```

## 覆盖模块

| 模块 | 假数据场景 | 结果 |
|---|---|---|
| 上报 | 首次写入、重复幂等写入、内部凭证 | 通过 |
| 今日统计 | Input/Output/Total、成功失败、平均响应时间 | 通过 |
| 四周期汇总 | 今日、本周、本月、历史 | 通过 |
| 趋势 | 7 天、30 天、小时、自定义、缺失日期补零 | 通过 |
| 分页 | 26 条数据，第一页 20 条、第二页 6 条 | 通过 |
| 调用详情 | 输入/输出长度、模型、Token、响应时间、状态 | 通过 |
| 使用率 | 90% 使用率、剩余额度、高使用状态 | 通过 |
| 每用户额度 | 相同使用量，不同额度得到不同状态 | 通过 |
| 空状态 | 汇总、趋势、记录均返回 `empty=true` | 通过 |
| 数据隔离 | 其他客服无法读取记录详情 | 通过 |
| 刷新一致性 | 新增 24 条记录后今日汇总从 18 更新为 66 | 通过 |
| 异常 | 无认证、非法范围、上游额度凭证 | 通过 |
| C++ 契约 | DeepSeek、文本长度、Token、认证头序列化 | 通过 |

Java 使用 `-Xlint:all -Werror`，C++ 使用 `-Wall -Wextra -Wpedantic -Werror`。

## 未在本机执行

本机未安装 PostgreSQL 客户端和服务，因此未执行真实 PostgreSQL 集成测试。PostgreSQL DDL 和 JDBC 代码已纳入严格 Java 编译；部署前仍需在项目测试数据库执行迁移与连接测试。
